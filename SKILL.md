---
name: knowledge-asset-health-check
description: 对本地企业知识库资料目录执行不使用大模型的知识资产盘点与体检，生成离线 HTML 报告、文档整改清单和完整文档台账。用户要求体检、盘点或诊断本地知识库文档文件夹，或要求生成这三项成果时使用；不用于评价单篇文档的业务正确性。
---

# 知识资产体检

对用户指定的本地目录运行内置规则引擎，并交付三个本地文件。体检判断不调用大模型，原文档不上传。

## 输入

- 只要求一个待体检目录。
- 企业名称和输出目录是选填项；用户未提供时不要追问。
- 如果用户说“这个文件夹”但没有可解析的具体路径，再请求目录路径。

## 执行

从本 `SKILL.md` 所在目录解析脚本路径。macOS/Linux 优先使用 `python3`，Windows 优先使用 `py -3.12`，不可用时再尝试 `python`：

```text
python <skill-dir>/scripts/run_health_check.py <source-dir> [--company <name>] [--output <output-dir>]
```

脚本首次运行时，如果本机缺少解析组件，会在用户缓存目录建立隔离的 Python 环境并下载依赖；此过程不读取或上传待体检文档。后续运行复用该环境。

不要自行打开、读取、摘要或上传待体检目录中的文档；除非用户另有要求，只运行脚本并使用其最终 JSON 结果。不要用大模型重新判断脚本已经给出的敏感信息、入库状态或知识库规划。

默认在源目录旁边建立带时间的结果目录，不覆盖旧结果。不要传入 `--overwrite`，除非用户明确要求覆盖既有成果。

执行失败时，根据脚本错误说明处理一次明确可恢复的问题。不要绕过路径保护，不要把输出目录放到待体检目录内部。

## 返回

成功后简要说明检查文件数和三类入库判断数量，并提供以下三个绝对路径：

- `知识资产体检报告.html`
- `文档整改清单.xlsx`
- `完整文档台账.xlsx`

不要把文档正文或敏感命中原文复制到对话中。用户要求了解机器接口、退出码或兼容范围时，读取 [references/output-contract.md](references/output-contract.md)。
